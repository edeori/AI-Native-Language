# Local Runners

This directory is reserved for deterministic, local execution helpers.

## Purpose

- parse semantic markdown
- validate syntax and structure
- normalize into canonical graph JSON
- compute diffs
- run smoke tests
- cache artifacts

## Design principle

- local first
- deterministic where possible
- cloud only when necessary

